# Task Manager App

A simple Python console app to manage daily tasks.

## Features
- Add new tasks
- View all tasks
- Delete existing tasks
- Saves tasks permanently in `data/tasks.json`

## Run
```bash
python -m task_manager_app.main
